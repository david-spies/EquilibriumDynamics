import React from "react";
import ReactDOM from "react-dom/client";
import EquilibriumDashboard from "./Dashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EquilibriumDashboard />
  </React.StrictMode>
);
